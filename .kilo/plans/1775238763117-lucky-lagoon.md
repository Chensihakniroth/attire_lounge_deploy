# Task 1: Identify Group and Trigger Matrix
- Modify: `resources/js/components/pages/admin/PosProductManager.jsx`
    - Update `handleEditClick` to detect if the selection is a single group. If so, populate `matrixData` and set view to form with `mode: 'bulkMatrixEdit'`.

# Task 2: Update Matrix Grid for Editing
- Modify: `resources/js/components/pages/admin/PosProductManager.jsx`
    - Update `MatrixGrid` to accept `initialData` (prop or context) to populate stock values if editing.

# Task 3: Update Submit for Matrix Bulk Edit
- Modify: `resources/js/components/pages/admin/PosProductManager.jsx`
    - Update `handleSubmit` to check for `bulkMatrixEdit` mode.
    - If active, filter updated matrix values and send bulk request to the update endpoint.

# Task 4: Verify and Build
- Run: `npm run build`
